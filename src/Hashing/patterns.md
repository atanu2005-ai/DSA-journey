# Hashing Patterns

## 1. Frequency Count
Use when:
- Counting occurrences

Example:
- Count frequency of elements

---

## 2. Prefix Sum + HashMap
Use when:
- Subarray sum problems

Example:
- Subarray sum = K

---

## 3. Set for duplicates
Use when:
- Detect duplicates quickly